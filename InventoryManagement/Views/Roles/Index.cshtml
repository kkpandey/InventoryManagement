@{

}

<!-- SELECT2 EXAMPLE -->
<div class="box box-default">
    <div class="box-header with-border">
        <h3 class="box-title">Roles</h3>
        <div class="box-tools pull-right">
            <a href="@Url.Action("Create")" class="btn btn-circle btn-info" data-toggle="tooltip" title="Add Roles">
                <span class="btn-circle">
                    <i class="fa fa-plus"></i>
                </span>
            </a>
        </div>
    </div>
    <!-- /.box-header -->
    <div class="box">
        <div class="box-body">
            <table id="RoleGridViewData" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Role Name</th>
                        <th>Status</th>
                        <th>Action</th>                        
                    </tr>
                </thead>
                <tbody>
                 
                </tbody>
            </table>
        </div>
        <!-- /.box-body -->
    </div>
</div>
@section Scripts {
<script>
    $(function () {
        $('#RoleGridViewData').DataTable()
   
  });
    </script>
   
}